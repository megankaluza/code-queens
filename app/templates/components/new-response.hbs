{{#if addNewResponse}}
  <h1>answer me</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="answer">Reply: </label>
        {{input value=answer id="answer"}}
      </div>

      <div class="form-group">
        <label for="user">Username: </label>
        {{input value=user id="user"}}
      </div>


      <button {{action 'saveResponse1'}}>Save</button>

    </form>
  </div>
{{else}}
  <button {{action 'responseFormShow'}}>Reply</button>
{{/if}}
